# =============================================================================
# Dependências do backend (com limites de versão para builds reprodutíveis).
# As dependências pesadas de IA local (torch/transformers) ficam em
# requirements-ml.txt — só são necessárias para o fallback MedASR local.
# =============================================================================

# Django Core
Django>=4.2,<5.0
djangorestframework>=3.14,<3.16
django-cors-headers>=4.3,<4.5
django-filter>=23.0,<25.0

# Database
psycopg2-binary>=2.9,<3.0
dj-database-url>=2.1,<3.0

# FHIR
fhirclient>=4.2,<5.0

# HTTP & APIs
requests>=2.31,<3.0

# Auth & Security
PyJWT>=2.8,<3.0
cryptography>=41.0,<44.0

# Config
python-decouple>=3.8,<4.0

# Production Server
gunicorn>=21.0,<23.0

# Task Queue
celery>=5.3,<6.0
redis>=5.0,<6.0

# PDF & Documents
reportlab>=4.0,<5.0
Markdown>=3.4,<4.0

# System Monitoring
psutil>=5.9,<7.0

# Utilities
python-dateutil>=2.8,<3.0

# Audio I/O (leve)
soundfile>=0.12,<0.14

# RAG — leitura de PDF dos manuais clínicos (ingestão)
pypdf>=4.0,<6.0
